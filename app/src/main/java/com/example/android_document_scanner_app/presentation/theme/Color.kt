package com.example.android_document_scanner_app.presentation.theme

import androidx.compose.ui.graphics.Color

// Фоны
val BgPrimary     = Color(0xFFF7F4EF) // тёплый off-white — основной фон
val BgSecondary   = Color(0xFFEFEBE3) // карточки, инпуты
val BgTertiary    = Color(0xFFE8E2D6) // разделители, бордеры

// Поверхности
val Surface       = Color(0xFFFDFBF8) // модалки, Bottom Sheet
val SurfaceRaised = Color(0xFFFFFFFF) // поднятые карточки

// Текст
val TextPrimary   = Color(0xFF1C1917) // тёплый почти-чёрный
val TextSecondary = Color(0xFF6B6560) // вторичный текст
val TextTertiary  = Color(0xFFA09890) // плейсхолдеры

// Акцент (aged gold)
val Accent        = Color(0xFF8B7355)
val AccentLight   = Color(0xFFC4A882)
val AccentSubtle  = Color(0xFFF0E8DC) // фон за акцентными элементами

// Семантика
val Danger        = Color(0xFF8B3A3A) // бордовый — удаление, ошибки
val Success       = Color(0xFF4A7C59) // приглушённый зелёный
